# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real secrets.

# Phase 3: URL of the Pi's metering JSON endpoint (used at build time or client-side fetch)
# PUBLIC_METERING_URL=https://your-domain.example/api/metrics
